rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Read public files by default; tighten later
      allow read: if true;
      // Writes only for authenticated users
      allow write: if request.auth != null;
    }
  }
}

